{
  "name": "Auto-Order-SingleUse",
  "version": "1.0.0",
  "description": "AutoOrder Single Use",
  "main": "index.js",
  "scripts": {
    "start": "npm --prefix web run start -- -p 9000",
    "bot": "node index.js",
    "dev": "npm --prefix web run dev -- -p 9000",
    "dashboard": "npm --prefix web run dev -- -p 9000",
    "build": "npm --prefix web run build",
    "web:start": "npm --prefix web run start -- -p 9000"
  },
  "dependencies": {
    "@starvale-sdk/svalepay": "^1.1.3",
    "chalk": "^4.1.2",
    "qrcode": "^1.5.1",
    "telegraf": "^4.9.0"
  }
}